{
  "name": "clearframe",
  "version": "0.5.0",
  "private": true,
  "description": "Highlights biased language with neutral alternatives.",
  "scripts": {
    "build": "node build.js"
  },
  "dependencies": {
    "web-ext": "^10.0.0"
  }
}
