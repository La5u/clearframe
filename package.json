{
  "name": "clearframe",
  "version": "0.6.0",
  "private": true,
  "description": "Highlights authorial choices with neutral alternatives.",
  "scripts": {
    "build": "node build.js",
    "test": "node test.js"
  },
  "dependencies": {
    "web-ext": "^10.0.0"
  }
}
