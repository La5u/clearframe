{
  "name": "clearframe",
  "version": "0.1.0",
  "private": true,
  "description": "Browser extension to expose framing, euphemisms, and propaganda patterns.",
  "scripts": {
    "build": "node scripts/build-dictionary.js",
    "check": "node scripts/build-dictionary.js --check"
  },
  "engines": {
    "node": ">=18"
  }
}
